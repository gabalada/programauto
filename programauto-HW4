#include <stdio.h>

int main(void) {
for (int i=0; i<10; i++)  
  printf("%s\n", tab[i]);

int sum=0; 
  int i;

  for (i=0; i<1000; i++){

    if (i%2==0)
      sum += i;
  }
  printf("A soma de todos os números é: %d\n", sum);
  return 0;
}
